<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Barber Booking</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <header class="header">
      <div class="wrap header-inner">
        <h1 class="brand">Barber Studio</h1>
        <a href="#book" class="btn btn-ghost">Book now</a>
      </div>
    </header>

    <main>
      <section class="hero">
        <div class="wrap hero-inner">
          <div class="hero-copy">
            <h2 class="title">Look sharp. Feel confident.</h2>
            <p class="subtitle">Book a <strong>30-minute</strong> slot between <strong>10:00–20:00</strong>. Choose your barber, pick a date, and time.</p>
            <a class="btn" href="#book">Reserve a slot</a>
          </div>
          <div class="hero-card">
            <form id="appointmentForm" class="form" action="/appointments/book" method="POST">
              <div class="row">
                <label>
                  <span>Name</span>
                  <input type="text" name="name" placeholder="Your name" required />
                </label>
                <label>
                  <span>Email</span>
                  <input type="email" name="email" placeholder="you@example.com" required />
                </label>
              </div>
              <div class="row">
                <label>
                  <span>Phone</span>
                  <input type="tel" name="phone" placeholder="Your phone" required />
                </label>
                <label>
                  <span>Service</span>
                  <select name="service" required>
                    <option value="Haircut">Haircut</option>
                    <option value="Beard Trim">Beard Trim</option>
                    <option value="Haircut + Beard">Haircut + Beard</option>
                  </select>
                </label>
              </div>
              <div class="row">
                <label>
                  <span>Barber</span>
                  <select name="barber" id="barberSelect" required>
                    <option value="" disabled selected>Choose a barber…</option>
                  </select>
                </label>
                <label>
                  <span>Date</span>
                  <input type="date" name="date" id="dateInput" required />
                </label>
                <label>
                  <span>Time</span>
                  <select name="time" id="timeSelect" required disabled>
                    <option value="" disabled selected>Select a time…</option>
                  </select>
                </label>
              </div>
              <div class="actions">
                <button type="submit" class="btn">Book</button>
                <div id="formMessage" role="status" aria-live="polite"></div>
              </div>
            </form>
          </div>
        </div>
      </section>
    </main>

    <footer class="footer">
      <div class="wrap">
        <p>© <span id="year"></span> Barber Studio</p>
      </div>
    </footer>

    <script src="/js/main.js" defer></script>
  </body>
  </html>
